apply plugin: 'java'

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
    compile 'org.json:json:20090211'

    testCompile("junit:junit:4.12")
}

test {
    useJUnit()

}